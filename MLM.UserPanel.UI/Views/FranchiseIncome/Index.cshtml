
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="franchiseIncomeHeading">
    <div class="panel">
        <div class="panel-heading">
            <h3><i class="fa fa-handshake"></i>&nbsp;Franchise</h3>
        </div>
    </div>
</section>
<section class="franchiseIncomes">
    <div class="panel">
        <div class="panel-heading">
            <h4><i class="fa fa-handshake"></i>&nbsp;Franchise Requests</h4>
            <div class="pull-right">
                <a href="/FranchiseIncome/AddFranchise">New Frenchise Request</a>
            </div>
        </div>
        <div class="panel-body">
            <table class="table table-bordered white-bg">
                <thead>
                    <tr>
                        <th>Sr No</th>
                        <th>Total Pins Request</th>
                        <th>Free Pin(s)</th>
                        <th>Total Amount</th>
                        <th>Show Pins</th>
                    </tr>
                </thead>
                <tbody>
                    <tr ng-repeat="fit in data">
                        <td>{{$index+1}}</td>
                        <td>{{fit.Pins}}</td>
                        <td>{{fit.FreePins}}</td>
                        <td>{{fit.Amount}}</td>
                        <td><a href="/Pins?oPin={{fit.FranchiseIncomeTypeID}}" class="btn btn-sm btn-success">View</a></td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</section>

